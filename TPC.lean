-- TPC.lean
-- Main entry point: imports all submodules in topological order
-- Tensor product formulation: M = (ℤ, ⊛) ⊗_Mon (ℤ, ⋆), N = ⟨{k ⊗ k}⟩

import TPC.Basic
import TPC.Monoid
import TPC.Diagonal
import TPC.Norm
import TPC.Topology
import TPC.Main
